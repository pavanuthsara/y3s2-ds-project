const Home = () => {
  return (
    <section className="simple-page">
      <h1>Healthcare Platform Frontend</h1>
      <p>
        This workspace uses a feature-based structure so each group member can build independently.
        Visit Doctor Availability to manage doctor schedule slots through the backend API.
      </p>
    </section>
  );
};

export default Home;
